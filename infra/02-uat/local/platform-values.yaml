# Platform chart overrides for local Docker Desktop testing.
# Kong runs as LoadBalancer locally (Docker Desktop exposes it on localhost).
# ClusterSecretStore is skipped (no IRSA / AWS locally).

externalSecrets:
  enabled: false

aws:
  region: us-east-1
