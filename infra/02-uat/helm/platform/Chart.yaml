apiVersion: v2
name: uat-platform
description: UAT platform wiring — IRSA-friendly ClusterSecretStore for External Secrets (TLS + edge routing live on Terraform ALB + Kong)
type: application
version: 0.1.0
appVersion: "0.1.0"
