replicaCount: 1

image:
  # Overridden by Argo CD ApplicationSet (see infra/02-uat/README.md).
  repository: shooter
  tag: uat-latest

service:
  port: 80
  targetPort: 80

ingress:
  enabled: true
  # Traefik registers this IngressClass. Overridable for other controllers.
  className: traefik
  hosts:
    - uat.shmup.ettukube.com
  annotations: {}
