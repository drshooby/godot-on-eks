replicaCount: 1

image:
  # Overridden by Argo CD ApplicationSet (see infra/02-uat/README.md).
  repository: shooter
  tag: uat-latest

service:
  port: 80
  targetPort: 80

httpRoute:
  enabled: true
  hostnames:
    - uat.shmup.ettukube.com
  attachHttpsSection: false

gateway:
  name: uat-public
  namespace: uat
